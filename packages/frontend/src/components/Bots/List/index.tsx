import React, {FunctionComponent} from 'react';
import styles from './styles.module.scss';

const Index: FunctionComponent = (props) => {
  return (
    <div className={styles.BotsList}></div>
  );
}

export default Index;
